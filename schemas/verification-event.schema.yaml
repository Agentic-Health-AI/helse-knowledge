schema: helse-evidence/verification-event
version: "0.1"
record_type: verification-event
required: [id, record_type, actor_type, action, target_id, outcome, timestamp, provenance]
types: {id: string, record_type: string, actor_type: string, action: string, target_id: string, outcome: string, timestamp: string, provenance: object}
enums:
  actor_type: [llm, validator]
  action: [parse, audit, reduce, validate]
